import styled from "styled-components";

export const Container = styled.div`
  margin: 2rem;
  margin-bottom: 0;
  button {
    width: 4rem;
    height: 2rem;
    background: var(--white);
    font-weight: 600;
    color: var(--title);
  }
`;
